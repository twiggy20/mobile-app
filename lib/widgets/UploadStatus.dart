import 'package:firebase_storage/firebase_storage.dart';
import 'package:flutter/cupertino.dart';

// Widget buildUploadStatus(UploadTask task) => StreamBuilder(stream: task.snapshotEvents,builder: (BuildContext context, AsyncSnapshot snapshot) {
//   if (snapshot.hasData) {
//     final snap = snapshot.data;
//     final progress = sna
//   }
// });